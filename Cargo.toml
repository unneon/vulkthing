[package]
name = "vulkthing"
version = "0.1.0"
edition = "2021"

[dependencies]
ash = "0.37"
ash-window = "0.12"
image = { version = "0.24", features = ["png"], default-features = false }
nalgebra-glm = "0.18"
raw-window-handle = "0.5"
tobj = { version = "3.2", default-features = false }
winit = { version = "0.28", features = ["wayland"], default-features = false }
