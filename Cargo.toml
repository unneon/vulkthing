[package]
name = "vulkthing"
version = "0.1.0"
edition = "2021"

[dependencies]
ash = "0.37"
ash-window = "0.12"
raw-window-handle = "0.5"
winit = { version = "0.28", features = ["wayland"], default-features = false }
